# Worklog: RunnersHi-Server — 2026-02-21

---

## 19:02

### 요청사항
- BE-088 계정 탈퇴 정책 구현 Step 1: Google/Kakao revoke 로직 추가

### 작업 내용
- GoogleClient에 revokeToken() 추가 (access token 미저장이므로 로그만 남기고 skip)
- KakaoClient에 unlinkUser() 추가 (Admin Key 방식, POST /v1/user/unlink)
- application.yaml에 oauth.kakao.admin-key 환경변수 설정 추가
- UserService.withdraw()에 provider별 OAuth revoke 분기 로직 추가
- 모든 revoke는 실패해도 탈퇴 진행 (try-catch + 로그)

### 변경 통계
```
 auth/client/GoogleClient.kt      | 11 +++++++
 auth/client/KakaoClient.kt       | 37 +++++++++++++++++++++-
 domain/user/service/UserService.kt | 25 ++++++++++++++-
 application.yaml                  |  2 ++
 4 files changed, 73 insertions(+), 2 deletions(-)
```

---

## 19:04

### 요청사항
- BE-088 계정 탈퇴 정책 구현 Step 2: 탈퇴 유예 및 계정 복구 로직

### 작업 내용
- UserStatus에 DELETED 상태 추가 (30일 경과 후 개인정보 삭제 완료 상태)
- AuthService.processLogin()에 WITHDRAWN 사용자 처리 분기 추가
  - 유예 기간(30일) 내 로그인 시 계정 복구 (status=ACTIVE, deletedAt=null)
  - 유예 기간 초과 시 USER_NOT_FOUND 에러 (배치에서 처리 예정)
  - DELETED 상태는 providerId 마스킹으로 조회 불가하지만 안전장치 추가

### 변경 통계
```
 domain/auth/service/AuthService.kt | 47 +++++++++--
 domain/user/entity/UserStatus.kt   |  3 +-
 2 files changed, 41 insertions(+), 9 deletions(-)
```

---

## 19:06

### 요청사항
- BE-088 계정 탈퇴 정책 구현 Step 3: 배치 스케줄러 - 30일 경과 계정 정리

### 작업 내용
- SchedulingConfig.kt 생성 (@EnableScheduling)
- WithdrawnUserCleanupScheduler.kt 생성 (매일 새벽 3시 실행)
  - WITHDRAWN + deletedAt 30일 초과 유저 조회
  - 연관 데이터 삭제 (RunningRecord, UserMission, UserLevelSnapshot, TermsAgreement)
  - 개인정보 마스킹 (nickname, email, profileImageUrl, fcmToken, providerId)
  - 상태를 DELETED로 변경
- User 엔티티: providerId, email을 var로 변경 (마스킹을 위해)
- UserRepository에 findByStatusAndDeletedAtBefore() 추가
- 각 Repository에 deleteAllByUserId() 추가 (RunningRecord, UserMission, UserLevelSnapshot)

### 변경 통계
```
 common/config/SchedulingConfig.kt                  |  8 +++
 user/scheduler/WithdrawnUserCleanupScheduler.kt    | 68 ++++++++++++++++++
 user/entity/User.kt                                |  4 +-
 user/repository/UserRepository.kt                  |  2 +
 level/repository/UserLevelSnapshotRepository.kt    |  2 +
 mission/repository/UserMissionRepository.kt        |  2 +
 running/repository/RunningRecordRepository.kt      |  2 +
 7 files changed, 86 insertions(+), 2 deletions(-)
```

---

## 19:08

### 요청사항
- BE-088 계정 탈퇴 정책 구현 Step 4: 단위 테스트 추가

### 작업 내용
- UserServiceTest 수정: GoogleClient/KakaoClient mock 추가, createUser()에 provider 파라미터 추가
  - Kakao 유저 탈퇴 시 unlinkUser 호출 확인
  - Google 유저 탈퇴 시 revokeToken 호출 확인 (로그만 남김)
  - Apple 유저 탈퇴 시 서버 revoke 없이 탈퇴 진행
  - Kakao/Google revoke 실패해도 탈퇴 정상 진행 확인
- AuthServiceTest 수정: 탈퇴 유저 계정 복구 테스트 추가
  - WITHDRAWN 유저 30일 이내 로그인 시 계정 복구 (ACTIVE, deletedAt null)
  - WITHDRAWN 유저 30일 초과 로그인 시 복구 불가 (USER_NOT_FOUND)
  - 30일 경계값 테스트
- WithdrawnUserCleanupSchedulerTest.kt 신규 생성
  - 30일 경과 유저 개인정보 마스킹 확인
  - 연관 데이터 삭제 확인 (RunningRecord, UserMission, LevelSnapshot, TermsAgreement)
  - 정리 대상 없을 때 아무 작업 안 함 확인
  - 한 유저 실패해도 다른 유저 정리 계속 진행 확인

### 변경 통계
```
 auth/service/AuthServiceTest.kt                     |  77 ++++++++++++++++++
 user/service/UserServiceTest.kt                     |  93 +++++++++++++++++++-
 user/scheduler/WithdrawnUserCleanupSchedulerTest.kt | 175 +++++++++++++++++++++ (신규)
 3 files changed, 342 insertions(+), 3 deletions(-)
```
---

## 22:02 (auto)

### 변경된 파일 (3개)
```
src/main/kotlin/com/runnershi/domain/user/scheduler/WithdrawnUserCleanupScheduler.kt
src/main/kotlin/com/runnershi/domain/user/scheduler/WithdrawnUserCleanupService.kt
src/test/kotlin/com/runnershi/domain/user/scheduler/WithdrawnUserCleanupSchedulerTest.kt
```

### 변경 통계
```
 .../scheduler/WithdrawnUserCleanupScheduler.kt     | 35 +----------------
 .../user/scheduler/WithdrawnUserCleanupService.kt  | 44 ++++++++++++++++++++++
 .../scheduler/WithdrawnUserCleanupSchedulerTest.kt | 10 +++--
 3 files changed, 52 insertions(+), 37 deletions(-)
```

